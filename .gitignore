node_modules/
.DS_Store
*.log

# Operator-local state, never committed: diagnostic logs, backups of a live
# install, and anything captured from a real session.
logs/
backups/
captures/
generated-images/
private-docs/
*.jsonl
